# Omni Tuya Local v0.5.45

- Removed the empty Submit confirmation screen from Options Flow cloud sync.
- Selecting cloud sync in Options now runs it immediately, closes the dialog, and creates a persistent Home Assistant notification with the full result.
- Selecting cloud sync from Add Integration now runs the sync immediately instead of opening an empty confirmation form.
